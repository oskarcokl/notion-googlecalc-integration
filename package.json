{
  "name": "notion-googlecalc-integration",
  "version": "1.0.0",
  "description": "Notion integratino that sync google calendar with notion calendar",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "coskar",
  "license": "ISC",
  "dependencies": {
    "@notionhq/client": "^2.2.2"
  },
  "devDependencies": {
    "dotenv": "^16.0.3"
  }
}
